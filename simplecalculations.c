#include <stdio.h>

int main(){
	int testcases;
	int n;
	int cases = 1;

	scanf("%d",&testcases);

	while(1){
		if(testcases == 0)
			break;

		scanf("%d",&n);


		while(1){
			if(a > b)
				break;

			sum += a;
			a += 2;
		}

		printf("Case %d: %d\n", cases, sum);

		testcases--;
		cases++;
	}
	return 0;
}