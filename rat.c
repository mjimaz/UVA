#include <stdio.h> //Standard I/O library
#include <math.h>

#ifndef max
	#define max( a, b ) ( ((a) > (b)) ? (a) : (b) )
#endif

#ifndef min
	#define min( a, b ) ( ((a) < (b)) ? (a) : (b) )
#endif

int main(){
	int scenario;


   printf("hello");



   scanf("%d",&scenario);

   printf("scenatio %d", scenario);

   return 0;
}